# Папка для логов

В этой папке хранятся файлы логов.

В учебном режиме (по умолчанию) сюда пишутся отправляемые email-уведомления:
- `emails.log` — все email-сообщения (создаётся автоматически)

Чтобы переключиться на реальную отправку через SMTP, отредактируйте `config/mail.php`.
